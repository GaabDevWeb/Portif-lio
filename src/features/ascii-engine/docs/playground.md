# Playground

Efeitos via `emitField` na superfície da engine.

## Ready

matrix, ripple, smoke, gravity, fire, water, wind, explosion, noise, particle-field, tornado, cloth

## Contrato

`PlaygroundEffect.mount(InfluencerSurface) → { stop }`
