# Presets & Themes

Presets versionados (`schemaVersion: 1`) com save/dup/export/import.

Themes: DOS, CRT, Linux, Amber, IBM, GameBoy, Windows XP, Matrix, Mono, ROOT OS — CSS variables mapeadas para tokens do shell.
