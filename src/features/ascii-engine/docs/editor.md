# Editor

`EditorDocument` com layers, selection, clipboard, undo/redo.

## Tools
select (ready); brush/eraser/fill/stamp/text/transform (stubs com descriptors).

## Limites
Não pinta células ainda — histórico cobre config + matrix swap.
