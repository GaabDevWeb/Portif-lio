# Converters

Registry de `SourceAdapter` por formato.

## Ready
- image (HTMLImageElement)
- gif (File)

## Stubs
video, webcam, canvas, clipboard, screen, svg, pdf

## Fluxo
`findFor(input)` → `convert(options, onProgress)` → matrix | animation
