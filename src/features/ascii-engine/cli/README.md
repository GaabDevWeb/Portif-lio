# CLI Ready

Estrutura preparada para um binário Node futuro.

## Comandos

| Comando | Estado |
|---------|--------|
| `convert` | stub |
| `export` | stub |
| `play` | stub |
| `benchmark` | stub |
| `info` | stub |

Ver `commands.ts` para synopses.

## Dependências futuras

- Reutilizar `ConverterRegistry` e exporters Blob-first
- Adapter filesystem (sem `document.createElement`)
- Package bin: `ascii-engine`
