# Phase log — P2

**Branch:** `ascii-engine-platform`  
**Date:** 2026-07-09  
**Status:** done

## Entrega

- `editor/commands.ts` — `EditorCommand` + `CommandHistory` (max 64)
- `editor/matrix-ops.ts` — clone/patch imutável + flood fill
- `editor/tools.ts` — brush / eraser / fill / selection commands
- `editor/document.ts` — `EditorDocument` com `applyCommand`, `paintAt`, `eraseAt`, `fillAt`
- Compat: mutações legadas (`setLayerMatrix`, …) via snapshot-as-command
- `EDITOR_TOOLS`: select/brush/eraser/fill → `ready`
- Studio: `EditorToolsPanel` mínimo
- Test: `editor-document.test.ts` (paint + undo 20 passos)

## Verificação

- `npx tsc --noEmit`
- `npx vitest run src/features/ascii-engine/editor/editor-document.test.ts`
- eslint nos paths tocados

## Critério de done

- [x] Command pattern (não só snapshots)
- [x] Brush / eraser / fill mutam células
- [x] Undo ≥20 passos
- [x] Selection via comando
- [x] Docs P2 + editor.md
