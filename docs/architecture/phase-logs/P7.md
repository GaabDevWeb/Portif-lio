# Phase log — P7

**Branch:** `ascii-engine-platform`  
**Date:** 2026-07-09  
**Status:** done

## Entrega

UI mínima de node graph na tab **Studio**, persistindo no `ProjectDocument`.

### A. NodeGraphPanel (área principal)
- `src/labs/ascii/studio/NodeGraphPanel.tsx`
- Lista / add / remove nodes (catálogo built-in P6)
- Edges via dropdowns (from/fromPort → to/toPort) — **sem** canvas library
- **Run graph** — `createNodeGraphRunner` + buffer demo em `ImageSource` bindings
- **Save to project** — `projectDoc.setNodeGraph(graph)`
- Reload / Clear; log de validação/execução

### B. Shell Studio
- `AsciiLab.tsx`: área principal `tab=studio` → `NodeGraphPanel` (substitui EmptyCanvas)
- Sidebar: `NodeGraphSidebarHint` (contagem nodes/edges do projeto) + painéis existentes

### C. Docs
- SSOT §1.1 / §1.5 / §3.13 atualizados
- Este phase-log

## Verificação

- `npx tsc --noEmit`
- eslint nos paths tocados

## Critério de done

- [x] UI mínima (lista + edges form) na Studio
- [x] Run graph (headless runner P6)
- [x] Save via `setNodeGraph`
- [x] phase-logs/P7.md
- [x] Sem Hero / sem merge main / aditivo
