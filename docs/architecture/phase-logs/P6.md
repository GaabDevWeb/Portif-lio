# Phase log — P6

**Branch:** `ascii-engine-platform`  
**Date:** 2026-07-09  
**Status:** done

## Entrega

### A. Tipos + runner headless
- `nodes/types.ts` — `NodeGraph`, portas (`ImageBuffer|RgbaFrame[]|AsciiMatrix|AsciiAnimation|Blob`), defs
- `nodes/validate.ts` — validação DAG + ordem topológica (Kahn)
- `nodes/runner.ts` — `NodeGraphRunner` com memoização opcional por hash

### B. Built-in nodes (16 ≥ 12)
ImageSource, Resize, Brightness, Contrast, Gamma, Exposure, Blur, Sharpen, Edge, Threshold, Invert, Dither, CharsetMap, ColorMode, Effect, Export.

Reutilizam `applyImageFilters`, `sampleRgba`, `applyDithering`, charset-mapper, `matrixToAsciiSource` / `matrixToJson`.

### C. Integração
- `createAsciiEngine().nodes` → `NodeGraphRunner` (substitui `null`)
- `NodeGraphStub` → alias de `NodeGraph` (compat aditiva, padrão P4)
- Export público via `ascii-engine/nodes` + barrel

### D. Testes
- `nodes/nodes.test.ts` — ciclo rejeitado; source→brightness→charset→export → AsciiMatrix + Blob; round-trip ProjectDocument; SDK wiring

## Verificação

- `npx tsc --noEmit`
- vitest: `nodes/nodes.test.ts`
- eslint nos paths tocados

## Critério de done

- [x] NodeGraphRunner headless
- [x] ≥12 nodes built-in
- [x] DAG validation + topological execute
- [x] Memoização por hash
- [x] Wire createAsciiEngine().nodes
- [x] ProjectDocument NodeGraph real
- [x] Unit test pipeline graph
- [x] Docs + phase-logs/P6.md
- [x] Sem UI de nodes (P7) / sem Hero / sem merge main
