# Phase log — P4

**Branch:** `ascii-engine-platform`  
**Date:** 2026-07-09  
**Status:** done

## Entrega

### A. KeyframeTrack + ops
- `animator/types.ts` — `KeyframeTrack`, `PropertyKeyframe`, `TrackInterpolation` (`hold|linear`), properties `opacity|offsetX|offsetY|charsetDensity`, `TimelineDocument`
- `animator/keyframe-ops.ts` — `addKeyframe` / `removeKeyframe` / `updateKeyframe` / `upsertTrack` (imutáveis)
- Frame ops existentes (`duplicateFrame`, `insertBlankFrame`, `removeFrame`, `mergeFrames`) preservados

### B. Sampling + blend
- `sampleTrack(timeOrFrame, track)` — hold vs linear entre keys
- `blendMatrices(a, b, t)` — lerp cell-wise de luminance/RGB + nearest charset glyph (demo)

### C. Onion skin
- `getOnionSkinLayers` — prev/next matrices + opacity weights
- `composeOnionPreview` — composite demo via blend
- Toggle **Onion** na timeline do tab Animate (`AnimationTimeline` + `AnimationResultView`)

### D. ProjectDocument
- `TimelineDocumentStub` → alias de `TimelineDocument` (compat aditiva)
- `normalizeTimeline` no `fromJSON` aceita stub legado `{ keyframes }`

## Verificação

- `npx tsc --noEmit`
- vitest: `animator/animator.test.ts` (mid-point, hold, 10-frame demo, blend, onion)
- eslint nos paths tocados

## Critério de done

- [x] KeyframeTrack + interpolation hold|linear
- [x] Ops imutáveis
- [x] sampleTrack
- [x] blend luminance maps (demo)
- [x] Onion skin helper + UI toggle
- [x] TimelineDocument no ProjectDocument
- [x] Docs P4
