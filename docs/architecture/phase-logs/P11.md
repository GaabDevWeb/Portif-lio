# Phase log — P11

**Branch:** `ascii-engine-platform`  
**Date:** 2026-07-09  
**Status:** done

## Entrega

AI adapter stubs (`AiProvider`) + heatmap de luminância em Stats — sem chamadas de rede.

### Módulo `ai/`
| Peça | Função |
|------|--------|
| `AiProvider` | Contrato §3.15: `promptToAscii`, `suggestCharset`, `suggestParams`, `enhance`, `reverseAscii`, `ocrAscii` |
| `StubAiProvider` / `defaultAiProvider` | Todos os métodos → `{ status: "stub" }` (sem fetch) |
| `ThrowingAiProvider` | Fail-fast opcional para testes / UI disabled |
| `createAsciiEngine({ ai? })` | Default stub; App injeta provider real |

### Stats heatmap (§3.11)
- `buildLuminanceHeatmap(matrix)` → `values` Float32Array + min/max/mean/coverage
- `formatHeatmapPreview` — downsample ASCII
- `buildStatsPanelModel.heatmap` preenchido
- StatsPanel: secção “Luminance heatmap” (métricas + preview)

### Ficheiros
- `src/features/ascii-engine/ai/{types,stub-provider,index,ai.test}.ts`
- `src/features/ascii-engine/stats/{heatmap,index,stats.test}.ts`
- `src/features/ascii-engine/sdk/create-ascii-engine.ts`
- `src/labs/ascii/stats/StatsPanel.tsx` (só secção heatmap)
- `src/features/ascii-engine/docs/{ai,stats-benchmark,sdk-cli}.md`
- SSOT §1.1 / §3.11 / §3.15

## Verificação

- `npx tsc --noEmit`
- `npx vitest run src/features/ascii-engine/ai src/features/ascii-engine/stats`
- eslint nos paths tocados

## Critério de done

- [x] AiProvider stubs (6 métodos)
- [x] `createAsciiEngine().ai` ≠ null (stub)
- [x] Heatmap stats + StatsPanel
- [x] Sem rede no Core/SDK
- [x] Docs + phase-log P11
- [x] Sem tocar Hero / sem merge main
