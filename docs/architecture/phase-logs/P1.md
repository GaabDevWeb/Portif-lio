# Phase log — P1

**Branch:** `ascii-engine-platform`  
**Date:** 2026-07-09  
**Status:** done

## Entrega

- `document/` — `ProjectDocument` + types v3.0
- `storage/` — `ProjectStore` (IDB) + `exportProjectZip` / `importProjectZip`
- SDK: `createAsciiEngine().document` + `.storage`
- Catalogs: export/import `project`
- Studio: `ProjectPanel` (Save IDB / Export / Import)
- `EditorDocument.hydrate` + `setActiveLayerId` (additive)
- Test: `project-document.test.ts` (JSON + ZIP round-trip)

## Verificação

- `npx tsc --noEmit` OK
- `npx vitest run src/features/ascii-engine/document/project-document.test.ts` — 2 passed

## Critério de done

- [x] Round-trip projeto ZIP
- [x] IDB put/get/list
