# Phase log — P5

**Branch:** `ascii-engine-platform`  
**Date:** 2026-07-09  
**Status:** done

## Entrega

Playground: ≥4 stubs → `ready` via `emitField` / `InfluencerSurface` (mesmo contrato dos efeitos existentes).

### Novos efeitos ready
| id | Comportamento |
|----|---------------|
| `fire` | Rajadas ascendentes a partir da base |
| `wind` | Fluxo horizontal com rajadas (sin gust) |
| `particle-field` | Nuvem de impulsos curtos com velocidades aleatórias |
| `explosion` | Onda expansiva radial periódica |
| `water` | Bandas de ondulação horizontal com deriva |
| `noise` | Campo turbulento pseudo-aleatório contínuo |

### Intactos (já ready)
- matrix, ripple, smoke, gravity

### Stubs restantes
- tornado, cloth

### Ficheiros
- `src/features/ascii-engine/playground/index.ts` — implementações + registry
- `src/labs/ascii/playground/PlaygroundPanel.tsx` — copy UI
- `docs/architecture/ASCII-ENGINE-PLATFORM.md` §1.1 / §3.6

## Verificação

- `npx tsc --noEmit`
- eslint nos paths tocados
- smoke: `defaultPlaygroundRegistry.list()` → 10 ready / 2 stub

## Critério de done

- [x] ≥4 stubs implementados como ready
- [x] Contrato `mount → { stop }` preservado
- [x] Registry + lab UI atualizados
- [x] Docs SSOT + phase log P5
- [x] Sem tocar Hero / sem merge main
