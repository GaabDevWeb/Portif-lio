# Phase log — Scene Editor W3

**Status:** done

## Entrega
- `tools/` — ToolHost, SelectionModel, brush/pencil/eraser/fill/move/hand/zoom
- `brush/` — BrushPreset serializável, BrushEngine stamp, presets (ready + experimental)
- SceneHistory no commit de stroke / erase / move / fill
- Toolbar na tab Edit

## Gate
- [x] Tools independentes (sem import cruzado)
- [x] Stamp determinístico com seed
- [x] Testes: brush stamp + tool registry
