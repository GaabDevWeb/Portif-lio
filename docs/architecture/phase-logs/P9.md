# Phase log — P9

**Branch:** `ascii-engine-platform`  
**Date:** 2026-07-09  
**Status:** done

## Entrega

PluginHost + charset pack exemplo — load sem rebuild do Core (same-origin).

### Módulo `plugins/`
| Peça | Função |
|------|--------|
| `PluginManifest` / `PluginModule` | Contrato §3.14 |
| `CharsetRegistry` | Built-ins (`IMAGE_CHARSETS`) + contribuições |
| `ThemeRegistry` | Built-ins + contribuições opcionais |
| `PluginHost.load(manifest, module)` | Regista charsets/themes; `activate` opcional |
| `examples/charset-pack` | shade-blocks, braille-dense, geometric, dots-extra |

### SDK
- `createAsciiEngine().plugins` → instância `PluginHost` (já não `null`)
- Opção `plugins?: PluginHost` para injectar host isolado em testes

### Studio (opcional)
- `PluginsPanel` — botão “Load charset pack” na tab Studio

### Docs
- `src/features/ascii-engine/docs/plugins.md`
- SSOT §1.1 / checklist implícito via phase-log

## Verificação

- `npx tsc --noEmit`
- `npx vitest run src/features/ascii-engine/plugins/plugin-host.test.ts`
- eslint nos paths tocados

## Critério de done

- [x] PluginHost + manifest
- [x] 1 plugin exemplo charset
- [x] Wire em `createAsciiEngine().plugins`
- [x] Load sem alterar Core / Hero
- [x] Testes: host carrega exemplo
- [x] Sem merge main; same-origin only
