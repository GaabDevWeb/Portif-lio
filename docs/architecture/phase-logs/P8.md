# Phase log — P8

**Branch:** `ascii-engine-platform`  
**Date:** 2026-07-09  
**Status:** done

## Entrega

Um converter novo end-to-end: **SVG rasterizado** + **batch stub** (API + UI).

### SVG (ready)
- `rasterizeSvgToImage` — File/Blob/markup → `Image` → canvas `drawImage` → PNG blob → `HTMLImageElement`
- `SvgAdapter` — `canHandle` `.svg` / `image/svg+xml` / markup; `convert` → `runImagePipeline`
- Registado em `ConverterRegistry` com `status: "ready"`
- Convert tab: `ImageUploadZone` aceita SVG (além de PNG/JPG/WEBP)

### Batch (stub)
- `convertBatchStub(files)` — devolve `status: "stub"` + lista de itens
- Opção `processReady` documentada para processar sequencialmente via adapters ready (não usada na UI stub)
- UI: secção "Batch (stub)" em `ImageConverterPanel`
- ZIP/pasta de saída: **não** implementado (explícito no stub)

### Ficheiros
- `src/features/ascii-engine/converters/{types,rasterize-svg,svg-adapter,batch-stub,index}.ts`
- `src/features/ascii-engine/converters/converters.test.ts`
- `src/labs/ascii/image/{ImageUploadZone,ImageConverterPanel,BatchConvertStub}.tsx`
- `src/features/ascii-engine/docs/converters.md`
- `docs/architecture/ASCII-ENGINE-PLATFORM.md` §1.1 / §3.1

## Verificação

- `npx tsc --noEmit`
- eslint nos paths tocados
- `npx vitest run src/features/ascii-engine/converters/converters.test.ts`
- Rasterize E2E (canvas.toBlob) é browser-only — não coberto em jsdom

## Critério de done

- [x] 1 fonte nova end-to-end (SVG)
- [x] Registry marca svg ready
- [x] UI Convert aceita SVG
- [x] Batch stub UI/API documentado
- [x] phase-log P8
- [x] Sem tocar Hero / sem merge main
