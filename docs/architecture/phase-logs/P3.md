# Phase log — P3

**Branch:** `ascii-engine-platform`  
**Date:** 2026-07-09  
**Status:** done

## Entrega

### A. Image worker path
- `image-pipeline/workers/image.worker.ts` — RGBA→matrix via `convertRgbaFrameToMatrix`
- `image-pipeline/workers/image-worker-client.ts` — sample no main + transfer + `runImagePipelineAsync`
- `useImagePipeline` — async worker com fallback sync
- `runImagePipeline` permanece API pública sync

### B. patchSource dims-stable
- `CharacterGrid.patchFromMatrix` / `patchFromSource` / `canPatchFrom`
- `AsciiInteractionEngineCore.patchSource` → patch + `evolution.syncAllHome` ou `setSource`
- `AsciiInteractionEngine` prefere `patchSource` quando engine existe
- Sparse com `count` diferente → fallback `setSource`

### C. Multi-worker GIF pool
- `ConversionWorkerPool` com N workers (`min(N, hardwareConcurrency)`, default N=4)
- `partitionBatch` round-robin; aggregate; cancel/destroy all
- Protocolo `convert-batch` / progress / batch-result inalterado
- Fallback main-thread inalterado

## Verificação

- `npx tsc --noEmit`
- vitest: `character-grid.patch.test.ts`, `worker-pool.test.ts`
- eslint nos paths tocados

## Critério de done

- [x] Image worker + fallback
- [x] patchSource dims-stable
- [x] GIF pool N workers
- [x] Docs P3
