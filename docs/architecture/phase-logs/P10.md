# Phase log — P10

**Branch:** `ascii-engine-platform`  
**Date:** 2026-07-09  
**Status:** done

## Entrega

CLI Node mínima: `convert` | `info` | `benchmark` (SSOT §3.17 / P10).

### Entrypoint
| Peça | Função |
|------|--------|
| `cli/run.ts` | argv router; help; exit codes |
| `npm run ascii-engine` | `tsx src/features/ascii-engine/cli/run.ts` |
| `tsx` | devDep — resolve `@/` + TypeScript sem build intermédio |

### Comandos ready
| Comando | Implementação |
|---------|----------------|
| `info` | `createAsciiEngine()` → versão, converters, exporters, importers, plugins/charsets/themes |
| `benchmark` | `runNodeBenchmarkSuite` — fixture RGBA sintética + `convertRgbaFrameToMatrix` (sem DOM) |
| `convert` | `.gif` via `gifuct-js` + RGBA; `.txt` / `.json` (matrix, animation, project v3) → TXT/JSON |

### Stubs mantidos
`export`, `play`, `analyze`, `serve` — documented em `commands.ts` / help.

### Limites (sem deps pesadas)
- **PNG/JPEG/WEBP/SVG:** `sampleImage` / ImageAdapter usam canvas DOM → erro explícito no CLI.
- **Não** adicionado `sharp` / `pngjs` / `canvas` — justificado: `gifuct-js` já cobria GIF; TXT/JSON cobrem documentos; PNG fica browser/lab até fase futura.
- Benchmark browser (`runBenchmarkSuite(HTMLImageElement)`) intacto; CLI usa caminho RGBA paralelo.

### Ficheiros
- `src/features/ascii-engine/cli/{run,info,benchmark,convert,fs-io,commands,index}.ts`
- `src/features/ascii-engine/cli/README.md`
- `src/features/ascii-engine/docs/sdk-cli.md`
- `package.json` script + `tsx` devDep
- SSOT §1.5 lacunas (P10 marcado)

## Verificação

- `npm run ascii-engine -- info` (smoke OK)
- `npm run ascii-engine -- benchmark --width 40`
- `npm run ascii-engine -- convert <txt|gif> -o …`
- PNG → mensagem de limitação + exit 1

## Critério de done

- [x] CLI `convert|info|benchmark` corre em Node
- [x] npm script + tsx
- [x] phase-log P10
- [x] Sem Hero / sem merge main
- [x] Sem deps pesadas unjustificadas
