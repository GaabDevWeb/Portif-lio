# Phase log — Scene Editor W4

**Status:** done

## Entrega
- `LayersPanel` — list/rename/dup/hide/lock/reorder/delete
- `InspectorPanel` — x,y,w,h, opacity, visible, locked, name, effects
- Wired no sidebar da tab Edit

## Gate
- [x] Layers ops via SceneDocument + history
- [x] Inspector no objeto selecionado
